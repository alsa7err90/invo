 

        <a href="{{ $url }}" id="{{ $modal }}"
    class="settings" title="تحرير" data-id="{{ $id }}"
    data-bs-toggle="modal" data-bs-target="#{{ $target}}"><i
        class="material-icons">&#xe3c9;</i></a>
